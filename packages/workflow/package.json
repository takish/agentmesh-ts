{
  "name": "@agentmesh/workflow",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint src/",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@agentmesh/core": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^4.0.18"
  }
}
